name: build
description: Action to test the build and install procedures.

runs:
  using: "composite"
  steps:
    - name: Build Package
      run: |
        python -m pip install --upgrade pip build
        python -m build
      shell: bash
